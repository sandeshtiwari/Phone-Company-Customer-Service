# Required. Keep the real value only in .env; .env is ignored by Git.
OPENAI_API_KEY=

# Optional local-development overrides.
OPENAI_MODEL=gpt-5.6-luna
POSTGRES_PORT=5544
APP_PORT=8080
COOKIE_SECURE=false
